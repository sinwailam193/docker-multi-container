{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4",
    "react-router-dom": "4.3.1",
    "axios": "0.21.1"
  },
  "scripts": {
    "docker-dev:build": "docker build -f Dockerfile.dev -t client .",
    "docker-dev:run": "docker run -p 3000:3000 -v /app/node_modules -v $(pwd):/app client",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
